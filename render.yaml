services:
  - type: web
    name: mynewsletter-ai
    env: python
    buildCommand: |
      apt-get update && apt-get install -y --no-install-recommends tesseract-ocr poppler-utils
      pip install -r requirements.txt
    startCommand: gunicorn app:app

